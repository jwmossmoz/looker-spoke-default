# Dashboard 2861

`dashboards/tasks_overview_new.dashboard.lookml` defines dashboard 2861.
The entry in `lookml_dashboards.yaml` keeps its existing URL and syncs its
contents after merge to `main`. Review new UI edits before merge: the sync
replaces the saved dashboard definition.

This follows Mana's [dashboard workflow](https://mozilla-hub.atlassian.net/wiki/spaces/DATA/pages/1624376227/Best+Development+Practices#Workflow).
Validate the model and dashboard in Looker development mode before merge.
